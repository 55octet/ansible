- name: Install Postfix packages
  ansible.builtin.package:
    name:
      - postfix
      - postfix-pgsql

- name: Install Dovecot packages
  ansible.builtin.package:
    name:
      - dovecot
      - dovecot-pgsql
      - dovecot-pigeonhole
